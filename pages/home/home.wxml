
<view class="container">
  <view class="item" wx:for="{{contentlist}}" wx:for-index="idx" wx:for-item="joke" wx:key="joke.id">
     <text class="title">{{joke.title}}</text>
     <text class="time">{{joke.ct}}</text>
     <text>{{joke.text}}</text>
  </view>
</view>